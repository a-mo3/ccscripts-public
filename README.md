everything outside the pvmscripts module is very old and should be ignored, with the exception of ontology. which is probably the most important thing to look at.

the commit history is removed but this repo spans across ~4-5 years, a lot of things are done in ways i would not recommend now.
